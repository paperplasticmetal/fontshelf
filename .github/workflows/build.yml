name: macOS build
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - name: Toolchain
        run: xcodebuild -version
      - name: Build and regression checks
        run: ./build.sh
      - name: Verify local signature
        run: codesign --verify --deep --strict dist/FontShelf.app
      - name: Figma importer contract tests
        run: node tests/figma-import.test.js
      - name: Archive Store target without distribution signing
        run: >-
          xcodebuild -project FontShelf.xcodeproj -scheme FontShelf
          -configuration Release -derivedDataPath .build/xcode
          -destination 'generic/platform=macOS' -archivePath dist/FontShelf.xcarchive
          CODE_SIGNING_ALLOWED=NO archive
